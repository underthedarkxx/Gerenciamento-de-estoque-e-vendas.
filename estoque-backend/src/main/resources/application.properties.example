# ===================================================================
# ARQUIVO DE CONFIGURAÇÃO DE EXEMPLO
# ===================================================================
# Copie este arquivo para "application.properties" (na mesma pasta)
# e preencha com suas credenciais locais.
# O arquivo "application.properties" NÃO deve ser enviado ao GitHub.
# Certifique-se de que ele está no seu .gitignore.
# ===================================================================


# ==============================
# BANCO DE DADOS
# ==============================
# Substitua <nome_do_seu_banco> pelo nome do seu banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/<nome_do_seu_banco>?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Substitua pelas suas credenciais do MySQL
spring.datasource.username=<seu_usuario_mysql>
spring.datasource.password=<sua_senha_mysql>

# Driver do MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ==============================
# JPA / HIBERNATE
# ==============================
# Em desenvolvimento, pode ser útil usar "update" ou "create-drop".
# Em produção, o ideal é usar "none" ou "validate" e gerenciar o schema com migrations (Flyway/Liquibase).
spring.jpa.hibernate.ddl-auto=update

# Dialeto recomendado para versões recentes do MySQL e Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Mude para "true" em desenvolvimento para ver os comandos SQL gerados no console
spring.jpa.show-sql=false

# Boa prática para evitar problemas de performance e transação
spring.jpa.open-in-view=false


# ==============================
# SEGURANÇA / JWT
# ==============================
# A chave secreta para assinar os tokens JWT deve ser configurada como uma variável de ambiente.
# Exemplo: export JWT_SECRET="sua-chave-secreta-muito-longa-e-segura"
api.security.token.secret=${JWT_SECRET:fallback_secret_key_for_dev_only}


# ==============================
# CONFIGURAÇÕES DA APLICAÇÃO
# ==============================
# Porta em que a aplicação irá rodar
server.port=8080